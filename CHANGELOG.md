# Changelog

All notable changes to this project are recorded here.

## 0.1.2 - 2026-06-16

- Removed mihoro-cli's long-running stdout/stderr forwarding for mihomo.
- Removed `service logs` because mihomo core logs are handled by mihomo itself.
- Kept `service start` focused on starting mihomo, waiting for API readiness, and returning to the user.

## 0.1.1 - 2026-06-16

- Fixed `proxy enable` so it starts mihomo when the service is not already running.
- Added progress output for proxy mode, runtime config generation, mihomo startup, and system proxy setup.
- Ignored npm package tarballs generated by `pnpm pack`.

## 0.1.0 - 2026-06-16

- Initial standalone CLI for importing Clash Party config into mihoro data.
- Added subscription, service, system proxy, TUN, GeoData, node, and proxy group commands.
- Added npm-compatible Git and SSH installation instructions.
